paths:
  /api/tickets/{ticketId}/assignees:
    post:
      summary: Assigns a collaborator to the ticket.
      tags:
        - Tickets Assignments

      security:
        - bearerAuth: []

      parameters:
        - $ref: "#/components/parameters/AcceptLanguage"
        - in: path
          name: ticketId
          required: true
          schema:
            type: integer
          example: 1

      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - userId
              properties:
                userId:
                  type: integer
                  example: 5

      responses:
        "201":
          description: Collaborator successfully assigned to the ticket
          content:
            application/json:
              schema:
                type: object
                properties:
                  ticketId:
                    type: integer
                    example: 1

                  userId:
                    type: integer
                    example: 5

                  user:
                    $ref: "#/components/schemas/User"

        "400":
          description: Validation or business rule error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"

        "401":
          description: Invalid or expired token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Unauthorized"

        "403":
          description: No permission.

    get:
      summary: Lists the employees assigned to the ticket.
      tags:
        - Tickets Assignments

      security:
        - bearerAuth: []

      parameters:
        - $ref: "#/components/parameters/AcceptLanguage"
        - in: path
          name: ticketId
          required: true
          schema:
            type: integer
          example: 1

      responses:
        "200":
          description: List of staff members assigned to the ticket
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/User"

        "404":
          description: Ticket not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

  /api/tickets/{ticketId}/assignees/{userId}:
    delete:
      summary: Removes a collaborator from the ticket.
      tags:
        - Tickets Assignments

      security:
        - bearerAuth: []

      parameters:
        - $ref: "#/components/parameters/AcceptLanguage"
        - in: path
          name: ticketId
          required: true
          schema:
            type: integer
          example: 1

        - in: path
          name: userId
          required: true
          schema:
            type: integer
          example: 5

      responses:
        "200":
          description: Collaborator removed from the ticket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

        "404":
          description: Assignment not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

        "401":
          description: Invalid or expired token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Unauthorized"

        "403":
          description: No permission